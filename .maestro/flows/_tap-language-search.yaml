appId: ${MAESTRO_APP_ID}
---
# The search field keeps its own testID. The results list must not merge
# into the same accessibility node.
- extendedWaitUntil:
    visible:
      id: language-search
    timeout: 15000
- tapOn:
    id: language-search
    waitToSettleTimeoutMs: 0

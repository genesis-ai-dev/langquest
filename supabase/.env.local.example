EXPO_PUBLIC_SUPABASE_URL=http://LOCAL_IP:54321
# LangQuest website URL for auth redirects (running locally on port 3000)
AUTH_SITE_URL=http://LOCAL_IP:3000

# Resend API base URL override for local proxy
RESEND_BASE_URL=http://LOCAL_IP:4000
RESEND_API_KEY=re_test_key

# Local stack service role key. config.toml reads env(SERVICE_ROLE_KEY) at
# `supabase start` to seed the vault (supabase_service_role_key, used by
# handle_invite_trigger and process_storage_object_deletions) and
# edge_runtime.secrets.
SERVICE_ROLE_KEY=sb_secret_N7UND0UgjKTVK-Uodkm0Hg_xSvEMPvz